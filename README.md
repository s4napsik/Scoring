# Home Credit Default Risk Competition

В этом задании вы познакомитесь с тем, как обучать модели машинного обучения на реальной задаче предсказания рисков дефолта клиентов банка.

# Задание

## Регистрация

Зарегистрироваться на [kaggle](https://www.kaggle.com) и принять участие в [соревновании](https://www.kaggle.com/competitions/home-credit-default-risk/overview).

## Скачать данные и собрать из них датасет

### Как подключиться к SQL базе

#### DBeaver
- Скачиваем [DBeaver](https://dbeaver.io/download/)
- Подключаемся к базе данных postgreSQL. 

Хост: 82.202.137.136

база homecredit 

юзер ваш ник 

пароль который вам прислали

- Все свои таблицы создавайте в своей личной схеме со своим постфиксом. Напимер: schema_bogdan. К другим базам у вас либо не будет доступа на чтение либо только на чтение. Если это не так - напишите в [телеграме](https://t.me/inframaster) о проблеме.

#### python

с помощью метода pandas.read_sql
пример [кода](src/postgres_connection.py)

#### database homecredit
- schema dwh лежат исходные данные, которые есть на сайте kaggle. К ней имеют доступ на чтение все пользователи.
- все остальные личные schema выдаются каждому и доступ к ним имеет только ученик, так что в ней вы можете тестировать свой код без ограничений.

### менеджер пакетов python
Используем [uv](https://docs.astral.sh/uv/getting-started/installation/), он работает проще и быстрее, чем poetry. к тому же uv содержит встроенный pip установщик и менеджер версий питона.

установить uv
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```
указываем какую версию питона нужно скачать
```bash
uv python install 3.9.21
```
создать окружение в котором будут храниться библиотеки исключительно для этого проекта
```bash
uv venv
```

запустить скрипт
```bash
uv run src/postgres_connection.py
```

активировать окружение и запустить скрипт
```bash
source .venv/bin/activate
python src/postgres_connection.py
```

добавить библиотеку для работы в jupyter ноутбуках
```bash
uv add notebook
```


## провести EDA (exploration data analysis)

- Результат оформить в jupyter notebook. 
[Пример хорошего EDA](https://www.youtube.com/watch?v=LDSMqYSE1vI&list=PLaRUeIuewv8CMFox0oEjlyePUhUmo-x0h&index=30&t=2907s)


- **Составить 10 гипотез**, которые должны улучшить качество модели и протестировать их. Результат также оформить в jupyter notebook. Тестируем гипотезы локально и с помощью скрытого датасета по метрике в лидерборде на сайте. Перед тестированием гипотез сделайте бейзлайн (соберите простой датасет и обучите модель, чтобы была отправная точка).
В jupyter notebook ожидается увидеть скрин из лидерборда. Описание гипотез и результаты (локальные и с лидерборда). 
- Также важна воспроизводимость ваших экспериментов, поэтому за каждой гипотезой должен быть закреплен python файл, который собирает датасет и обучает модель. (Укажите их в jupyter notebook)


### Гипотезы
При тестировании любой гипотезы используйте:
- Автоматизированные методы подбора гиперпараметров, советую optuna
- Автоматизированные методы отбора фичей - shap values / permutation importance / PSI

Гипотеза в текущем задании - это предположение о том, что новая идея (фича или набор фичей) позволит улучшить качество модели на скрытом датасете на сайте
Гипотеза должна содержать логический смысл, который должен быть описан в результирующем ноутбуке.


### Советы
- Оформляйте код преобразования датасета в класс. Это сильно упростит вам жизнь и не придется разбираться как же вы получили результат.
- Оформляйте юпитер ноутбуки не в последний момент, а заранее, вам будет проще ориентироваться в коде.
- Работа с данными - это точный и скурпулезный процесс, чем акуратнее все будете делать, тем выше будет результат.
- Можете использовать [MLFLow](http://82.202.137.136:8000/) для логирования версий моделей.

##### проверка качества кода
Перед пушем убедитесь, что ваш код соответствует нормам кода. Для этого запустите линтеры.
```bash
uvx ruff format
uvx ruff check --fix
```

#### Ожидаемый результат

В качестве решения принимается merge-request на этот github репозиторий, где:
- в pyproject.toml добавлены все необходимые для работы вашего проекта библиотеки
- файл guide.md, в котором описана структура репозитория и функционал классов
- все файлы .py необходимые для работы моделей
- линтер, проверяющий что код соответствует кодстайлу выполнился без ошибок